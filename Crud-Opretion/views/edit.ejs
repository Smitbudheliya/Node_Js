<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Todo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom m-3 p-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="/show"><i class="bi bi-list-check"></i> My Todos</a>
        </div>
    </nav>

    <div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">
        <div class="col-md-6 glass-card p-4">
            <h3 class="mb-4 text-gradient text-center"><i class="bi bi-pencil-square"></i> Edit Todo</h3>

            <!-- enctype added for image upload -->
            <form action="/update/<%= todo._id %>" method="POST" enctype="multipart/form-data">
                
                <div class="mb-3">
                    <label class="form-label">Task</label>
                    <input type="text" name="task" class="form-control input-glass" value="<%= todo.task %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Description</label>
                    <textarea name="description" class="form-control input-glass"><%= todo.description %></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-semibold">Type</label>
                    <select name="type" class="form-select input-glass">
                        <option value="Open" <%=todo.type==="Open" ? "selected" : "" %>>Open</option>
                        <option value="In Progress" <%=todo.type==="In Progress" ? "selected" : "" %>>In Progress</option>
                        <option value="Review" <%=todo.type==="Review" ? "selected" : "" %>>Review</option>
                        <option value="Done" <%=todo.type==="Done" ? "selected" : "" %>>Done</option>
                    </select>
                </div>

                <!-- Image section -->
                <div class="mb-3">
                    <label class="form-label fw-semibold">Profile</label>
                    <% if(todo.image) { %>
                        <div class="mb-2">
                            <img src="<%= todo.image %>" style="width:60px; height:60px; border-radius:50%; object-fit:cover;">
                        </div>
                    <% } %>
                    <input type="file" name="image" class="form-control input-glass">
                </div>

                <div class="mb-3">
                    <label class="form-label">Category (comma separated)</label>
                    <input type="text" name="category" class="form-control input-glass"
                        value="<%= todo.category ? todo.category.join(', ') : '' %>">
                </div>

                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary w-50 me-2">Update</button>
                    <a href="/show" class="btn btn-secondary w-50">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</body>

</html>
